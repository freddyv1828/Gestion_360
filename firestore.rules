rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /administrador/{document} {
      allow read, write: if request.auth != null;
    }
    
    // Restringir el acceso a otras colecciones según tus necesidades
    // ...
  }
}
